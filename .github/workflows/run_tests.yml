name: Run Q-Room tests Manually

on:
  workflow_dispatch:  # Позволяет запускать вручную через GitHub Actions

jobs:
  test:
    runs-on: ubuntu-latest  # Используем Linux-среду

    steps:
      - name: 📥 Клонирование репозитория
        uses: actions/checkout@v3

      - name: 🔧 Установка Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: 🚀 Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install --with-deps

      - name: ✅ Запуск тестов Pytest + Playwright
        run: pytest #--alluredir=allure-results

#       - name: 📦 Сохранение Allure-отчета
#         uses: actions/upload-artifact@v3
#         with:
#           name: allure-report
#           path: allure-results
